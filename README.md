# Claude Code Dotfiles

个人 Claude Code 偏好配置，跨设备一键同步。

## 新电脑恢复

```bash
git clone https://github.com/ma-xue-feng/dotfiles.git ~/dotfiles
cd ~/dotfiles && bash setup.sh
```

setup.sh 自动完成：
1. 安装 CLAUDE.md 偏好配置
2. 安装 superpowers 插件（14 skills）
3. 安装 gstack（53 skills）

完成后自动删除 dotfiles 目录，不留多余文件。
